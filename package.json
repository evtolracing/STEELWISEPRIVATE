{
  "name": "steelwise-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SteelWise Steel Manufacturing & Distribution Platform",
  "scripts": {
    "dev": "npm --prefix src/backend run dev",
    "dev:frontend": "npm --prefix src/frontend run dev",
    "vercel-install": "npm install --prefix src/backend --legacy-peer-deps && npm install --prefix src/frontend --legacy-peer-deps",
    "vercel-build": "cd src/backend && npx prisma generate && cd ../frontend && npm run build",
    "build": "cd src/backend && npx prisma generate && cd ../frontend && npm run build",
    "start": "npm --prefix src/backend run start",
    "prisma:generate": "npm --prefix src/backend run db:generate",
    "prisma:migrate": "npm --prefix src/backend run db:migrate"
  },
  "keywords": ["steel", "erp", "manufacturing"],
  "author": "SteelWise",
  "license": "UNLICENSED",
  "engines": {
    "node": "20.x"
  }
}
